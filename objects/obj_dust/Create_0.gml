/// @description Variables and setup

// Dust variables
image_xscale = random_range(0.8, 1.2);
image_yscale = image_xscale;
rot_speed = random_range(0.5, 2);
fade_speed = random_range(0.025, 0.05);
drift_speed = random_range(1, 5);